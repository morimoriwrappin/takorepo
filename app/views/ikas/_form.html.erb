<%= form_for(@ika) do |f| %>
  <% if @ika.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ika.errors.count, "error") %> prohibited this ika from being saved:</h2>

      <ul>
      <% @ika.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :user_id %>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, size: "50x3" %>
  </div>
  <div class="field">
    <%= f.label :background %><br>
    <%= f.file_field :background %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
